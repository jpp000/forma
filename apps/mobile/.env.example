# Point at the Forma API (no trailing slash).
EXPO_PUBLIC_API_URL=http://localhost:3000

# OAuth mobile handoff — must match API OAUTH_MOBILE_SUCCESS_URL.
# Expo scheme is `forma` (see app.json); dev default uses expo-linking if unset.
EXPO_PUBLIC_OAUTH_SUCCESS_URL=forma://oauth
