# Expo / Mobile agents

Read the exact versioned Expo docs at https://docs.expo.dev/versions/v57.0.0/ before writing Expo API code.

## Design (required)

Before changing UI:

1. Read `/DESIGN.md` (project root)
2. Read `HANDOFF.md` (this app)
3. Use tokens from `src/design-systems/appleFitness.ts`
4. Reuse `src/components/apple-fitness/*`
5. Match accepted **Variant A** (`src/prototype/home/VariantA.tsx`) — not Wise/Shopify

**Brand:** primary green `#30D158` for CTAs/chrome. Move pink `#FA114F` only on the outer ring / energy accents.
